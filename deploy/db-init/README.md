# The production SQL dump is placed in this directory at deploy time (Phase 4).
# Files here run automatically on FIRST initialization of an empty db volume.
# This directory is intentionally kept in git; the .sql dump itself is git-ignored.
