# Copy this file to .env and fill in the values. .env is git-ignored.

# Password for the PostgreSQL "public_engagement_tool" role.
# Must match the password used in back-end/secret/db_url_production.
POSTGRES_PASSWORD=change-me
